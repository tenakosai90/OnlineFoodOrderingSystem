# Online Food Ordering System 🍔🍕

## 📌 Project Description
The **Online Food Ordering System** is a Java console application developed as a final project for the **Object-Oriented Programming (OOP)** course.  
It simulates a simple workflow where a customer views a restaurant menu, places an order, and completes payment.

This project demonstrates core OOP principles such as **encapsulation, inheritance, interfaces, and polymorphism**, and uses **Maven** and **JUnit** for build and testing.

---

## 🛠 Technologies Used
- Java 17
- Maven
- JUnit 5
- Git & GitHub

---

## ▶ How to Run the Application
From the project root (where `pom.xml` is):

```bash
mvn clean compile exec:java